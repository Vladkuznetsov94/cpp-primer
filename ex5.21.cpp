#include <iostream>
#include <vector>
int main()
{
    std::vector<std::string> text;
    std::string word, dup_word;
    while (std::cin >> word){
            text.push_back(word);
            for (auto index = 0; text[index].begin() + 1 == isupper() && text[index] == text[++index]; ++index)

            }
    }
    return 0;
    }
